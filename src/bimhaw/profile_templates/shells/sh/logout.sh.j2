#!/bin/sh

# this is the target file for adding in the various environment init
# scripts for the shell

# the names of the envs to source

logouts={{ sh_logouts }}

# source each env there
for logout in $logouts; do
    . "$BIMHAW_DIR/active/lib/sh/logouts/${logout}.sh"
done
